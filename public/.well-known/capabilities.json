{
  "version": "1",
  "invocation": { "http": "https://capabilitiestxt.org", "mcp": "https://capabilitiestxt.org/api/mcp" },
  "capabilities": [
    { "id": "registry.check", "version": "0.1.0", "description": "Check a capabilities.txt for conformance; returns a grade + recommendations", "invoke": "POST /api/check" },
    { "id": "registry.register", "version": "0.1.0", "description": "List a conformant site in the public directory", "invoke": "POST /api/register" },
    { "id": "registry.discover", "version": "0.1.0", "description": "Find which registered sites can do something", "invoke": "GET /api/discover?q=" },
    { "id": "registry.badge", "version": "0.1.0", "description": "Get a site's conformance badge as SVG", "invoke": "GET /api/badge?domain=" },
    { "id": "tools.validate", "version": "0.1.0", "description": "Validate a capabilities.txt file (CLI)" },
    { "id": "tools.generate_from_openapi", "version": "0.1.0", "description": "Generate a capabilities.txt from an OpenAPI document (CLI)" },
    { "id": "tools.resolve", "version": "0.1.0", "description": "Fetch and parse any site's capabilities.txt (CLI / MCP)" }
  ]
}
